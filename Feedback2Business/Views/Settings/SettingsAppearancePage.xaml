<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Feedback2Business.Views.Settings.SettingsAppearancePage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:shared="clr-namespace:Feedback2Business.Views.Shared"
    BackgroundColor="{StaticResource PageBackground}">

    <Grid ColumnDefinitions="220,*" Padding="24" ColumnSpacing="16">
        <shared:SettingsNavView Grid.Column="0" />

        <Grid Grid.Column="1" RowDefinitions="Auto,*">
            <Grid ColumnDefinitions="*,Auto" Margin="0,0,0,16">
                <Label Text="Udseende &amp; Tema" Style="{StaticResource PageTitleStyle}" />
                <Button Grid.Column="1" Text="Gem ændringer"
                        Command="{Binding SaveAppearanceSettingsCommand}"
                        Style="{StaticResource PrimaryButtonStyle}" />
            </Grid>

            <Border Grid.Row="1" Style="{StaticResource CardStyle}">
                <ScrollView>
                    <VerticalStackLayout Padding="24" Spacing="18">
                        <Border Style="{StaticResource InnerCardStyle}" Padding="16">
                            <VerticalStackLayout Spacing="10">
                                <Label Text="Farvetema &amp; Layout" FontAttributes="Bold" />
                                <Label Text="Visningstilstand" Style="{StaticResource FormLabelStyle}" />
                                <Picker ItemsSource="{Binding Themes}" SelectedItem="{Binding SelectedTheme}" />
                                <Label Text="Primær accentfarve (HEX)" Style="{StaticResource FormLabelStyle}" />
                                <Entry Text="{Binding PrimaryAccentColor}" />
                            </VerticalStackLayout>
                        </Border>

                        <Border Style="{StaticResource InnerCardStyle}" Padding="16">
                            <VerticalStackLayout Spacing="10">
                                <Label Text="Sidebar &amp; Tæthed" FontAttributes="Bold" />
                                <HorizontalStackLayout>
                                    <CheckBox IsChecked="{Binding DarkNavySidebar}" />
                                    <Label Text="Mørk navy sidebar (Standard)" VerticalOptions="Center" />
                                </HorizontalStackLayout>
                                <HorizontalStackLayout>
                                    <CheckBox IsChecked="{Binding CompactDensity}" />
                                    <Label Text="Kompakt visning i tabeller og lister" VerticalOptions="Center" />
                                </HorizontalStackLayout>
                            </VerticalStackLayout>
                        </Border>
                    </VerticalStackLayout>
                </ScrollView>
            </Border>
        </Grid>
    </Grid>
</ContentPage>
