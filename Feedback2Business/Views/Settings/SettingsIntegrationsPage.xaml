<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Feedback2Business.Views.Settings.SettingsIntegrationsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:shared="clr-namespace:Feedback2Business.Views.Shared"
    BackgroundColor="{StaticResource PageBackground}">

    <Grid ColumnDefinitions="220,*" Padding="24" ColumnSpacing="16">
        <shared:SettingsNavView Grid.Column="0" />

        <Grid Grid.Column="1" RowDefinitions="Auto,*">
            <Grid ColumnDefinitions="*,Auto" Margin="0,0,0,16">
                <Label Text="API &amp; Integrationer" Style="{StaticResource PageTitleStyle}" />
                <Button Grid.Column="1" Text="Gem ændringer"
                        Command="{Binding SaveIntegrationsSettingsCommand}"
                        Style="{StaticResource PrimaryButtonStyle}" />
            </Grid>

            <Border Grid.Row="1" Style="{StaticResource CardStyle}">
                <ScrollView>
                    <VerticalStackLayout Padding="24" Spacing="18">
                        <Border Style="{StaticResource InnerCardStyle}" Padding="16">
                            <VerticalStackLayout Spacing="10">
                                <Label Text="REST API Konfiguration" FontAttributes="Bold" />
                                <Label Text="API Endpoint URL" Style="{StaticResource FormLabelStyle}" />
                                <Entry Text="{Binding ApiEndpoint}" />
                                <Label Text="API Nøgle" Style="{StaticResource FormLabelStyle}" />
                                <Grid ColumnDefinitions="*,Auto" ColumnSpacing="12">
                                    <Entry Text="{Binding ApiKey}" IsReadOnly="True" />
                                    <Button Grid.Column="1" Text="Generer ny nøgle"
                                            Command="{Binding GenerateNewKeyCommand}"
                                            Style="{StaticResource SecondaryButtonStyle}" />
                                </Grid>
                            </VerticalStackLayout>
                        </Border>

                        <Border Style="{StaticResource InnerCardStyle}" Padding="16">
                            <VerticalStackLayout Spacing="10">
                                <Label Text="Webhooks &amp; Dataudveksling" FontAttributes="Bold" />
                                <Label Text="Webhook URL" Style="{StaticResource FormLabelStyle}" />
                                <Entry Text="{Binding WebhookUrl}" />
                                <Label Text="Synkroniseringsinterval (minutter)" Style="{StaticResource FormLabelStyle}" />
                                <Entry Text="{Binding SyncIntervalMinutes}" Keyboard="Numeric" />
                            </VerticalStackLayout>
                        </Border>
                    </VerticalStackLayout>
                </ScrollView>
            </Border>
        </Grid>
    </Grid>
</ContentPage>
